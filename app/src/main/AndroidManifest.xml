<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.ifmo.md.lesson8">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
        <activity
            android:name="ru.ifmo.md.lesson8.MainActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/title_activity_main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="ru.ifmo.md.lesson8.WeatherContentProvider"
            android:authorities="ru.ifmo.md.lesson8"
            android:exported="false" />

        <service
            android:name="ru.ifmo.md.lesson8.LoadWeatherService"
            android:exported="false" />

        <service
            android:name="ru.ifmo.md.lesson8.AddCityService"
            android:exported="false" />

        <activity
            android:name="ru.ifmo.md.lesson8.AddCityActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/title_activity_add_city"
            android:parentActivityName="ru.ifmo.md.lesson8.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ru.ifmo.md.lesson8.MainActivity" />
        </activity>
    </application>

</manifest>
